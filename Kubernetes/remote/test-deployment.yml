---
# EMS Server Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ems-server-deployment
  namespace: testing
  labels:
    app: ems-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ems-server 
  template:
    metadata:
      labels:
        app: ems-server  # ← Must match selector
    spec:
      containers:
      - name: ems-server
        image: baraamohamed/gradproj:ems_server_kube
        ports:
        - containerPort: 5500
        env:
        - name: NODE_ENV
          value: "kube-production"
        - name: DB_HOST
          value: "db-service"  # ← Add database connection
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "ems_db"
        - name: DB_USER
          value: "root"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: my-secret
              key: MYSQL_ROOT_PASSWORD

---
# EMS Client Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ems-client-deployment
  namespace: testing
  labels:
    app: ems-client
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ems-client
  template:
    metadata:
      labels:
        app: ems-client
    spec:
      containers:
      - name: ems-client
        image: baraamohamed/gradproj:ems_client
        ports:
        - containerPort: 3000
        env:


---
# Hospital Server Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hospital-server-deployment
  namespace: testing
  labels:
    app: hospital-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hospital-server
  template:
    metadata:
      labels:
        app: hospital-server
    spec:
      containers:
      - name: hospital-server
        image: baraamohamed/gradproj:hospital_server_kube
        ports:
        - containerPort: 8888
        env:
        - name: NODE_ENV
          value: "kube-production"
        - name: DB_HOST
          value: "db-service"  # ← Add database connection
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "ems_db"
        - name: DB_USER
          value: "root"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: my-secret
              key: MYSQL_ROOT_PASSWORD

---
# Hospital Client Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hospital-client-deployment
  namespace: testing
  labels:
    app: hospital-client
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hospital-client
  template:
    metadata:
      labels:
        app: hospital-client
    spec:
      containers:
      - name: hospital-client
        image: baraamohamed/gradproj:hospital_client
        ports:
        - containerPort: 3000
        env:


---
# Database Deployment (with persistence)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-deployment
  namespace: testing
  labels:
    app: db
spec:
  replicas: 1  # ← Only 1 replica for MySQL
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - name: db
        image: baraamohamed/gradproj:db
        ports:
        - containerPort: 3306  
        env:
        - name: MYSQL_DATABASE
          value: "ems_db"
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: my-secret
              key: MYSQL_ROOT_PASSWORD

---
# Hospital Client Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: testing
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 82   # EMS
        - containerPort: 81   # Hospital
        - containerPort: 80   # nginx do not use it for other purposes
        volumeMounts:                          # ← Correct way to mount ConfigMap
          - name: nginx-config-volume
            mountPath: /etc/nginx/nginx.conf
            subPath: nginx-conf-file
      volumes:                                 # ← Volumes defined at pod level
        - name: nginx-config-volume
          configMap:
            name: nginx-configmap